# Anime-Recomendation-model
"The objective of this project was to help users discover anime they are likely to enjoy. Instead of manually searching through thousands of titles, the recommendation system analyzes the features of an anime and recommends the most similar ones."

2. What algorithm did you use?

Primary Algorithm:

✅ Content-Based Recommendation System
✅ Cosine Similarity (Similarity Measurement)

Feature Engineering:

One-Hot Encoding for Genre
One-Hot Encoding for Type
Numerical features:
Episodes
Rating
Members (Popularity)

Libraries Used

Python
Pandas
NumPy
Scikit-learn
Matplotlib
Seaborn

3. What is the dataset size?

Your notebook reads the anime.csv dataset. I can't confirm the exact number of rows from the notebook alone because the dataset file itself isn't attached, but the project uses:

Dataset: anime.csv
Features Used: Genre, Type, Episodes, Rating, Members

If this is the commonly used Anime Recommendation dataset from Kaggle, it contains approximately:

12,000+ anime records
7 columns (with 5 features used for recommendation)

Only mention the exact number on your resume if your anime.csv has that many records. Otherwise, check the output of:
